schema: anvil.component/v1
type: agent
name: security-auditor
title: Security Auditor
version: 0.1.0
description: Security audit — OWASP top 10, injections, hardcoded secrets, vulnerable dependencies
targets:
  copilot:
    supported: true
  claude:
    supported: true
    model: sonnet
    tools: Read, Grep, Glob, Bash
  cursor:
    supported: true
entry:
  file: index.md
